package Streams.Operations;

/**
 * Created by moon on 10/12/2016.
 *
 * close(): release lock
 * flush(): write file from cache immediately
 * markSupported(), mark(int), reset()
 * skip(long)
 */
public interface Overview {
}
